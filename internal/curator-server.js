const express=require('express');const {Pool}=require('pg');const path=require('path');const fs=require('fs');const {hashPassword,verifyPassword,token}=require('./auth');const app=express();const PORT=process.env.PORT||10000;const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});app.use(express.json());app.get('/',(q,r)=>{const html=fs.readFileSync(path.join(__dirname,'curator','index.html'),'utf8').replace('</head>','<link rel="stylesheet" href="/bordeaux-rose-island.css"></head>');r.type('html').send(html)});app.use(express.static(path.join(__dirname,'curator')));app.get('/bordeaux-rose-island.css',(q,r)=>r.sendFile(path.join(__dirname,'bordeaux-rose-island.css')));
async function init(){await pool.query(`CREATE TABLE IF NOT EXISTS portal_users(id BIGSERIAL PRIMARY KEY,role TEXT NOT NULL CHECK(role IN ('curator','ceo')),full_name TEXT,phone TEXT,email TEXT NOT NULL UNIQUE,password_hash TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),last_login_at TIMESTAMPTZ)`);await pool.query(`CREATE TABLE IF NOT EXISTS portal_sessions(id BIGSERIAL PRIMARY KEY,token_hash TEXT PRIMARY KEY,user_id BIGINT NOT NULL REFERENCES portal_users(id) ON DELETE CASCADE,role TEXT NOT NULL,expires_at TIMESTAMPTZ NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);const legacyEmail=process.env.CURATOR_ADMIN_EMAIL;const legacyPassword=process.env.CURATOR_ADMIN_PASSWORD;if(legacyEmail&&legacyPassword&&legacyPassword!=='CHANGE_ME'){const x=await pool.query("SELECT id FROM portal_users WHERE email=$1",[legacyEmail.toLowerCase()]);if(!x.rowCount)await pool.query("INSERT INTO portal_users(role,full_name,email,password_hash,status) VALUES('curator',$1,$2,$3,'active')",['Amour Privé Curator',legacyEmail.toLowerCase(),await hashPassword(legacyPassword)]);}}
async function auth(req,role){const raw=req.headers.authorization?.replace(/^Bearer\s+/i,'');if(!raw)return null;const h=require('crypto').createHash('sha256').update(raw).digest('hex');const x=await pool.query("SELECT u.* FROM portal_sessions s JOIN portal_users u ON u.id=s.user_id WHERE s.token_hash=$1 AND s.role=$2 AND s.expires_at>NOW() AND u.status='active'",[h,role]);return x.rows[0]||null}
app.get('/health',(q,r)=>r.json({ok:true,service:'Amour Prive Independent Travel Curator Portal'}));
app.post('/api/signup',async(q,r)=>{try{const {fullName,phone,email,password,confirmPassword}=q.body||{};const e=String(email||'').trim().toLowerCase();if(!fullName||!e||!password||password.length<8||password!==confirmPassword)return r.status(400).json({error:'Enter your name, a valid email and a password of at least 8 characters; passwords must match.'});const exists=await pool.query('SELECT id FROM portal_users WHERE email=$1',[e]);if(exists.rowCount)return r.status(409).json({error:'An account with this email already exists. Please log in.'});const u=await pool.query("INSERT INTO portal_users(role,full_name,phone,email,password_hash,status) VALUES('curator',$1,$2,$3,$4,'active') RETURNING id,full_name,email,status",[String(fullName).trim(),String(phone||'').trim(),e,await hashPassword(password)]);r.status(201).json({user:u.rows[0]});}catch(e){r.status(500).json({error:'Unable to create account right now.'})}});
app.post('/api/login',async(q,r)=>{try{const e=String(q.body?.email||'').trim().toLowerCase(),p=String(q.body?.password||'');const u=(await pool.query("SELECT * FROM portal_users WHERE email=$1 AND role='curator'",[e])).rows[0];if(!u||!(await verifyPassword(p,u.password_hash)))return r.status(401).json({error:'Invalid credentials'});if(u.status!=='active')return r.status(403).json({error:`Account status: ${u.status}. Please contact Amour Privé.`});const raw=token(),h=require('crypto').createHash('sha256').update(raw).digest('hex');await pool.query("INSERT INTO portal_sessions(token_hash,user_id,role,expires_at) VALUES($1,$2,'curator',NOW()+INTERVAL '7 days')",[h,u.id]);await pool.query('UPDATE portal_users SET last_login_at=NOW(),updated_at=NOW() WHERE id=$1',[u.id]);r.json({token:raw,user:{id:u.id,fullName:u.full_name,email:u.email}});}catch(e){r.status(500).json({error:'Unable to sign in right now.'})}});
app.post('/api/logout',async(q,r)=>{const raw=q.headers.authorization?.replace(/^Bearer\s+/i,'');if(raw){const h=require('crypto').createHash('sha256').update(raw).digest('hex');await pool.query('DELETE FROM portal_sessions WHERE token_hash=$1',[h]);}r.json({ok:true})});
app.post('/api/change-password',async(q,r)=>{try{const u=await auth(q,'curator');if(!u)return r.status(401).json({error:'Unauthorized'});const {currentPassword,newPassword,confirmPassword}=q.body||{};if(!(await verifyPassword(String(currentPassword||''),u.password_hash)))return r.status(400).json({error:'Current password is incorrect.'});if(!newPassword||String(newPassword).length<8||newPassword!==confirmPassword)return r.status(400).json({error:'New password must be at least 8 characters and match confirmation.'});await pool.query('UPDATE portal_users SET password_hash=$1,updated_at=NOW() WHERE id=$2',[await hashPassword(newPassword),u.id]);await pool.query('DELETE FROM portal_sessions WHERE user_id=$1',[u.id]);r.json({ok:true,message:'Password changed. Please sign in again.'});}catch(e){r.status(500).json({error:'Unable to change password.'})}});
app.get('/api/me',async(q,r)=>{const u=await auth(q,'curator');if(!u)return r.status(401).json({error:'Unauthorized'});r.json({id:u.id,fullName:u.full_name,email:u.email,status:u.status})});
app.get('/api/data',async(q,r)=>{if(!await auth(q,'curator'))return r.status(401).json({error:'Unauthorized'});const [b,i,c]=await Promise.all([pool.query('SELECT * FROM curator_bookings ORDER BY created_at DESC'),pool.query('SELECT * FROM curator_inquiries ORDER BY created_at DESC'),pool.query('SELECT * FROM curator_credentials ORDER BY created_at DESC')]);r.json({bookings:b.rows,inquiries:i.rows,credentials:c.rows})});app.get('/api/resources',async(q,r)=>{if(!await auth(q,'curator'))return r.status(401).json({error:'Unauthorized'});r.json((await pool.query('SELECT * FROM curator_resources ORDER BY category,partner,title')).rows)});
init().then(()=>app.listen(PORT,'0.0.0.0',()=>console.log('Curator portal listening on '+PORT))).catch(e=>{console.error(e);process.exit(1)});