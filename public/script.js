const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('section,article').forEach(e=>{e.classList.add('reveal');io.observe(e)});const header=document.querySelector('header');addEventListener('scroll',()=>{header.style.backdropFilter=scrollY>50?'blur(14px)':'none';header.style.background=scrollY>50?'rgba(16,21,18,.86)':'linear-gradient(180deg,rgba(16,21,18,.88),transparent)'},{passive:true});document.querySelectorAll('.hero-bg,.pic,.quote-bg,.jpic').forEach(el=>{const bg=getComputedStyle(el).backgroundImage;const m=bg.match(/url\(["']?(.*?)["']?\)/);if(!m)return;const img=new Image();img.onerror=()=>el.remove();img.src=m[1]});